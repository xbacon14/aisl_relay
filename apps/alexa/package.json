{
  "name": "@relay/alexa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/local.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke": "tsx src/smoke.ts",
    "bundle": "esbuild src/lambda.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/lambda/index.js && cd dist/lambda && rm -f ../relay-alexa-lambda.zip && zip -q ../relay-alexa-lambda.zip index.js && echo built dist/relay-alexa-lambda.zip"
  },
  "dependencies": {
    "@relay/contract": "*",
    "ask-sdk-core": "^2.14.0",
    "ask-sdk-model": "^1.86.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "esbuild": "^0.28.2"
  }
}
